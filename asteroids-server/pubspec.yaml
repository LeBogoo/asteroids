name: asteroids_server
description: The backend server for a Multiplayer Asteroids game.
version: 1.0.0
publish_to: "none"

repository: https://github.com/LeBogoo/asteroids/asteroids-server

environment:
  sdk: ^3.5.4

dependencies:
  args: ^2.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_web_socket: ^3.0.0
  shelf_static: ^1.1.0
  packet_networking:
    path: ./packages/packet-networking